# QCT-001 验收记录

日期：2026-09-19。状态：本轮通过；尚未接通用户吞错入口，不表示整个功能完成。

## 修改

- FE 启动配置 enable_query_corruption_tolerance，代码默认 false，产品模板尚未开启。
- QueryCorruptionPolicy：仅用户只读本地 OLAP SELECT 和受支持 Pipeline；排除内部、写入、outfile、EXPLAIN、lake/connector/混合源、短路、Arrow、未开放 HTTP 和 raw。
- 跨 BE 运行时过滤控制通道尚未接入，含 remote target 的计划保持严格；不禁用过滤、不改写计划。
- TQueryOptions 可选字段 218；PQueryStatistics 字段 14 与 PTransmitChunkParams 字段 12 为可选单调标记，缺省 false。

## 实际验证

- 专属容器内 make -C gensrc -j2 成功，日志 logs/QCT-001/gensrc.log。
- 从 fe 目录执行 Maven reactor test：QueryCorruptionPolicyTest 共 10 项，Failures=0、Errors=0、Skipped=0，BUILD SUCCESS。
- 覆盖配置关闭不检查计划、默认协议值、正常 SELECT、内部/写入、EXPLAIN/outfile、短路/非 Pipeline、外部/lake/混合源、空扫描/非结果 sink、remote runtime filter、HTTP 开放门槛/raw、Arrow。
- git diff --check 通过。FE 主代码、全部测试源码编译及 checkstyle 随 reactor 执行成功。
- BE 独立 Release 单测目录已配置并开始全量构建；尚未完成，因此不记录 BE 单测通过。
- 未做真实文件损坏、JDBC/HTTP 集成和性能评估，分别属于后续轮次。

最终 FE 命令：

    mvn -B -pl fe-core -am -Dmaven.clean.skip=true -Dmaven.repo.local=/query-corruption-workspace/cache/maven -Dfe_ut_parallel=1 -DfailIfNoTests=false -Dtest=QueryCorruptionPolicyTest test

日志位于专属持久卷 /query-corruption-workspace/logs/QCT-001/fe-tests-final.log。

## 首次构建环境记录

- 源仓库是 shallow repository；直接 clone bundle 缺少浅历史边界，未成功。随后在空隔离仓库导入同一只读 shallow 边界，再 unbundle/switch，核验源码 SHA=005fd4fc1cb03c8b888013775cf0d95645129774 后同步本轮文件。未借用 Tenant-TTL 代码或缓存。
- 镜像缺 Ninja，仅在专属容器安装 ninja-build 1.10.1；未修改宿主或其他容器。
- 初次从仓库根使用 Maven -f 导致现有 checkstyle 相对头文件解析失败，改为 fe 工作目录。
- 单测首次编译发现 ExecPlan.getFragments 返回 ArrayList 而非 List，修正 mock 后上述 10 项通过。
- FE POM 含 initialize 阶段 auto-clean。首次空构建执行过它；未删除既有已完成编译资产。所有后续增量命令显式使用 -Dmaven.clean.skip=true，保留产物。
- BE：GCC 12.3、Release、MAKE_TEST=ON、USE_STAROS=OFF、GCOV=OFF、STARCACHE=ON、TENANN=OFF、JIT=ON，ut_build_Release 与未来生产目录分离；低并行 -j2。
- 检查时 VM 数据盘约 175 GiB 可用，源与缓存仅在 sr-query-corruption-4.0-build-cache-arm64。
